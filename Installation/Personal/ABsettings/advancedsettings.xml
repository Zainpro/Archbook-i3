<!-- Ferchito 3, 2.5, 2, 1.5, 1 GB MacBook Air-->
<advancedsettings>

  <network>
    <curlclienttimeout>500</curlclienttimeout> <!-- Timeout in seconds for libcurl (http/ftp) connections -->
    <curllowspeedtime>500</curllowspeedtime> <!-- Time in seconds for libcurl to consider a connection lowspeed -->
  </network>
  
<cache>
 <memorysize>708669603</memorysize>  <!-- number of bytes used for buffering streams in memory - 
    Buffering in RAM 3x free memory (use 2 GB) 1073741824, 891205713, 708669603, 536870912, 354334801
    When set to 0 the cache will be written to disk instead of RAM -->
  <buffermode>2</buffermode>  <!-- Choose what to buffer:
    0) Buffer all internet filesystems (like "2" but additionally also ftp, webdav, etc.) (default)
    1) Buffer all filesystems (including local)
    2) Only buffer true internet filesystems (streams) (http, etc.)
    3) No buffer -->
  <readfactor>10</readfactor> <!-- this factor determines the max readrate in terms of readbufferfactor * avg bitrate of a video file.
This can help on bad connections to keep the cache filled. It will also greatly speed up buffering. Default value 4.0. -->
</cache>

  <skiploopfilter>
    <skiploopfilter>8</skiploopfilter> <!-- loop filter to skip on h264 decoding -16, 0, 8,  -->
    <loglevel hide="true">-1</loglevel>
  </skiploopfilter>
    
  <gui>
    <algorithmdirtyregions>3</algorithmdirtyregions>
    <nofliptimeout>0</nofliptimeout>
  </gui>
  
  <videoscanner>
    <ignoreerrors>true</ignoreerrors>
   </videoscanner>
  
</advancedsettings>

